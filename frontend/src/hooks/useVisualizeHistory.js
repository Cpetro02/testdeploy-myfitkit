//import {useAuthContext} from './useAuthContext'
export const useVisualizeHistory = () => {
    //const {dispatch} = useAuthContext()

    const visualize = () => {
        //remove user from storage
        //localStorage.removeItem('user')

        //dispatch logout action
        //dispatch({type: 'LOGOUT'})
    }

    return {visualize}
}